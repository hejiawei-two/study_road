
v1.0 建议补充

· 错误处理：perror、errno 的使用，以及关键函数失败后的资源回滚（比如 socket 失败不继续 bind）。
· 信号处理：忽略 SIGPIPE（防止写已关闭的 socket 导致进程退出）。
· 资源管理：用 RAII 封装 socket 文件描述符（或至少确保异常路径能 close）。

v2.0 建议补充

· Epoll 事件类型：EPOLLONESHOT（避免多线程同时处理同一 fd）、边缘触发模式下非阻塞读写的 EAGAIN 处理。
· 线程池优雅停止：条件变量等待时如何安全退出（notify_all + 标志位）。
· 数据库连接池：简单实现一个连接池，避免每次请求都创建连接。

v2.5 建议补充

· 配置文件热加载：监听文件变化，不重启服务更新配置（可选，加分项）。
· 异步日志的缓冲设计：双缓冲机制，减少锁竞争（可简单描述）。
· Redis 数据结构选择：为什么用 hash 存用户信息、用 list 存离线消息，以及过期时间设置。

v3.0 建议补充

· gRPC 四种模式：简单 RPC、服务端流、客户端流、双向流（聊天室可以展示双向流）。
· etcd 的健康检查：租约 + 保活机制，以及服务端如何检测失效服务。
· 消息队列的可靠性：生产确认、消费确认、死信队列（至少知道概念）。
· 分布式唯一 ID：雪花算法或 Redis 自增（用于消息 ID）。

v3.5 建议补充

· Dockerfile 优化：多阶段构建、减小镜像体积。
· K8s 健康检查：liveness 和 readiness 探针的区别与配置。
· 链路追踪上下文传递：在 gRPC 的 metadata 中传递 trace id。
· 熔断限流算法：令牌桶、漏桶、滑动窗口的区别。

v4.0 建议补充

· 内存池实现：定长分配或伙伴算法，以及如何与 new/delete 对接。
· 无锁队列：基于 CAS 的环形队列（boost::lockfree::queue 或手写）。
· 零拷贝：sendfile、splice、MSG_ZEROCOPY 的使用场景。
· 性能压测工具：wrk、perf、bcc 工具集、火焰图生成。
· 高可用指标：SLA、SLO、MTBF、MTTR 的概念。
